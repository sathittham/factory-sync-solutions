# Use modern, strict peer-dependency resolution (npm 7+ default).
# Pinned here so a developer's global ~/.npmrc (e.g. legacy-peer-deps=true)
# can't strip peerDependencies from the lockfile and break `npm ci` in CI.
legacy-peer-deps=false
