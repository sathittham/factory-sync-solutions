# This repo uses pnpm workspaces — run `pnpm install`, not `npm install`.
# Auto-install peer deps so apps don't each have to redeclare every peer.
auto-install-peers=true
# Don't fail the whole install on benign peer-version mismatches.
strict-peer-dependencies=false
