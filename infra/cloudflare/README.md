# Cloudflare Infrastructure

Cloudflare-owned infrastructure for FactorySync Solutions.

| Area | Path |
|---|---|
| API gateway Worker | `workers/api-gateway` |
| R2 bucket/domain notes | `r2/buckets.md` |
| Public domain mapping | `domains.md` |

## Commands

```bash
npm run test:api-gateway
npm run deploy:api-gateway:staging
npm run deploy:api-gateway:prod
```
