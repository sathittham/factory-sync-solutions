---
import { LegalContent } from "../components/legal/LegalContent";
import Layout from "../layouts/Layout.astro";

const appUrl = import.meta.env.PUBLIC_APP_URL ?? "#";
const version = import.meta.env.PUBLIC_APP_VERSION ?? "dev";
---

<Layout
  title="ตั้งค่าคุกกี้ — FactorySync Solutions"
  description="จัดการการตั้งค่าคุกกี้และความเป็นส่วนตัวของคุณบน FactorySync Solutions"
  noindex={true}
>
  <LegalContent client:load page="cookie-settings" appUrl={appUrl} version={version} />
</Layout>
