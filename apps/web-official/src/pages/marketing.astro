---
import { LegalContent } from "../components/legal/LegalContent";
import Layout from "../layouts/Layout.astro";

const appUrl = import.meta.env.PUBLIC_APP_URL ?? "#";
const version = import.meta.env.PUBLIC_APP_VERSION ?? "dev";
---

<Layout
  title="นโยบายการตลาด — FactorySync Solutions"
  description="นโยบายการส่งข้อมูลการตลาดของ FactorySync Solutions"
>
  <LegalContent client:idle page="marketing" appUrl={appUrl} version={version} />
</Layout>
